name: Type Coverage
on:
  - pull_request
jobs:
  type-coverage:
    runs-on: oss-vm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - run: npm ci
      - name: Type compilation 
        run: npx tsc --noEmit
      - name: Type coverage (99%)
        run: npm run ts-coverage:ci
      - name: Type test coverage
        run: |
          cd scripts/check-type-test-coverage
          npm ci
          node index.js ../../packages
