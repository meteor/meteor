machine:
  node:
    version: 8

checkout:
  post:
    - git submodule update --init

dependencies:
  cache_directories:
    - "node_modules"
  override:
    - npm install

deployment:
  s3:
    branch: /^(master|version-.*)/
    commands:
      - npm run deploy
