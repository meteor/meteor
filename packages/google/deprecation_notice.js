console.warn();
console.warn('This package (google) has been deprecated.');
console.warn(
  "To use the `Google` symbol, use the `google-oauth` package " +
  "and import from it."
);
console.warn(
  "If you need the Blaze OAuth configuration UI, add " +
  "`google-config-ui` alongside `accounts-ui`."
);
console.warn();
